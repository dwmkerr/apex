<UserControl x:Class="Gallery.DragAndDrop.DragAndDropView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:apexControls="clr-namespace:Apex.Controls;assembly=Apex"
             xmlns:DragAndDrop="clr-namespace:Gallery.DragAndDrop"
             mc:Ignorable="d" 
             Style="{StaticResource GalleryItemStyle}"
             d:DesignHeight="600" d:DesignWidth="800">
    
    <apexControls:PaddedGrid Rows="Auto,Auto,*,Auto" Padding="4">

        <!-- Title section. -->
        <TextBlock Grid.Row="0" Style="{StaticResource Heading1}">DRAG &amp; DROP</TextBlock>
        <TextBlock Grid.Row="1" Style="{StaticResource Subtitle}">Drag and drop is a complicated area to work with. Fortunately, Apex makes 
            adding drag and drop functionality to applications more straightforward.</TextBlock>
        
    </apexControls:PaddedGrid>
</UserControl>
