<UserControl x:Class="Gallery.Behaviours.BehavioursView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:apexControls="clr-namespace:Apex.Controls;assembly=Apex"
             xmlns:Controls="clr-namespace:Gallery.Controls" mc:Ignorable="d" 
             Style="{StaticResource GalleryItemStyle}"
             d:DesignHeight="600" d:DesignWidth="800">
    
    <apexControls:PaddedGrid Rows="Auto,Auto,*,Auto" Padding="4">

        <!-- Title section. -->
        <TextBlock Grid.Row="0" Style="{StaticResource Heading1}">BEHAVIOURS</TextBlock>
        <TextBlock Grid.Row="1" Style="{StaticResource Subtitle}">Some behaviours are included in Apex to help with common
        development tasks.</TextBlock>
        
    </apexControls:PaddedGrid>
</UserControl>
