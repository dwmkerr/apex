<UserControl x:Class="Gallery.PathTextBox.PathTextBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:apexControls="clr-namespace:Apex.Controls;assembly=Apex"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <apexControls:ApexGrid Rows="Auto,*,Auto">
        
        <!-- First section, a description of the control. -->
        <TextBlock Grid.Row="0" TextWrapping="Wrap">
            The PathTextBox is a textbox that allows the user to press an elipses button to browse for a path to a file 
            on the file system.
        </TextBlock>
        
        <!-- Second section, the path text box. -->
        <apexControls:PathTextBox 
            x:Name="searchTextBox"
            Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="120" />
        
        <!-- Third section, search options.-->
        <StackPanel Grid.Row="2" Orientation="Horizontal">
        </StackPanel>
    </apexControls:ApexGrid>
</UserControl>
