<UserControl x:Class="Gallery.Popups.PopupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:apexControls="clr-namespace:Apex.Controls;assembly=Apex"
             xmlns:DragAndDrop="clr-namespace:Gallery.DragAndDrop"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Style="{StaticResource GalleryItemStyle}">

        <apexControls:PaddedGrid Rows="Auto,Auto,*,Auto" Padding="4">

            <!-- Title section. -->
            <TextBlock Grid.Row="0" Style="{StaticResource Heading1}">POPUPS</TextBlock>
            <TextBlock Grid.Row="1" Style="{StaticResource Subtitle}">Apex provides functionality to handle popups in WPF, Silverlight or WP7 applications.</TextBlock>

        </apexControls:PaddedGrid>
    </Grid>
</UserControl>