<UserControl x:Class="BaseApplication.ApplicationHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ImageBrush x:Key="BackgroundBrush" ImageSource="/VisualStudio2010;component/Resources/BackgroundBrush.bmp"  Viewbox="0,0,4,4" ViewboxUnits="Absolute"
                    Viewport="0,0,4,4" ViewportUnits="Absolute" TileMode="Tile" />
    </UserControl.Resources>
    <Grid Background="{StaticResource ResourceKey=BackgroundBrush}">
        <DockPanel>
            <TabControl DockPanel.Dock="Left" Margin="4" Width="200">
                <TabItem Header="Title" />
            </TabControl>
        </DockPanel>
    </Grid>
</UserControl>
