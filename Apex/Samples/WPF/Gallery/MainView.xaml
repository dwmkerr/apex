<UserControl x:Class="Gallery.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:cueTextBox="clr-namespace:Gallery.CueTextBox"
            xmlns:searchTextBox="clr-namespace:Gallery.SearchTextBox"
             xmlns:pivotControl="clr-namespace:Gallery.PivotControl" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl>
            <TabItem Header="CueTextBox">
                <cueTextBox:CueTextBoxView />
            </TabItem>
            <TabItem Header="SearchTextBox">
                <searchTextBox:SearchTextBoxView />
            </TabItem>
            <TabItem Header="PivotControl">
                <pivotControl:PivotControlView />
            </TabItem>
        </TabControl>
            
    </Grid>
</UserControl>
